apiVersion: v1
kind: Secret
metadata:
  name: stock-ai-runtime
  namespace: stock-ai
type: Opaque
stringData:
  database_url: postgresql+psycopg://USER:PASSWORD@POSTGRES_HOST:5432/stock_ai
  redis_url: rediss://:PASSWORD@REDIS_HOST:6379/0
  polygon_api_key: REPLACE_WITH_SECRET_VALUE
  auth_jwt_secret: OPTIONAL_FOR_RS256_JWKS
